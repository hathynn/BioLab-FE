//USER
export enum USER_API {
  REGISTER = "register",
  LOGIN = "login",
  REFRESH = "refresh",
}

export enum BRAND {
  DEFAULT = "brands"
}

export enum CATEGORY {
  DEFAULT = "categories"
}

export enum PRODUCT {
  DEFAULT = "products"
}